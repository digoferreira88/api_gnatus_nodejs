node_modules
.vscode
.DS_Store
.env
.claude/
# Backups da migracao MSSQL -> Postgres (nao versionar)
*.mssql.bak
# Debug helpers temporarios
debug_*.js
# Storage de uploads (cobranca/anexos, etc) — nao versionar
uploads/
pipe_cards.json
scripts/_out/
# Chaves privadas (TOTVS Identity .pk8, etc) — NUNCA versionar
secrets/
*.pk8

# Certificados/chaves NUNCA no repo (auditoria 30/08/2026 — chave privada ficou rastreada por meses)
certificados/
*.key
*.pem
*.crt
*.pfx
*.p12
